
# Disable Directory Listing
Options -Indexes

# Block access to sensitive files
<FilesMatch "^\.env|^\.git|composer\.json|composer\.lock">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect includes directory from direct access if possible, 
# but usually we just protect specific extensions or rely on code checks.
# Preventing access to any php file in include/ might be too aggressive if they are accessed via AJAX?
# Let's stick to the basics first.

# Block access to hidden files (starting with dot)
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
